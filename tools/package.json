{
  "name": "my-portfolio",
  "private": true,
  "description": "Tooling for Krishal Maharjan's portfolio: builds the accessible resume PDFs. The site itself has no build step.",
  "scripts": {
    "build:resumes": "node build-resumes.cjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
