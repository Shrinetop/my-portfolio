{
  "name": "my-portfolio",
  "private": true,
  "description": "Krishal Maharjan's portfolio. Static site; the only tooling is the accessible resume PDF build.",
  "scripts": {
    "build:resumes": "node tools/build-resumes.cjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
